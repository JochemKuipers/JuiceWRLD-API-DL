# Environment notes

- Machine-wide facts (gh CLI, uv, PowerShell, git push behavior) live in the
  global rules at `Documents\Cline\Rules\environment.md`.
- Run tests with `.venv\Scripts\python.exe -m pytest -q`.
